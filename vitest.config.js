/**
 * vitest.config.js — тайм-аут под сэмплирующую геометрию.
 *
 * Дефолтные 5000ms валят тяжёлые интеграционные тесты (дифференциал по 444 SVG,
 * validateAnatomy по 67 вариантам) под CPU-нагрузкой параллельных агентов:
 * в простое они укладываются в секунды, под конкуренцией — десятки секунд.
 * Диагноз 2026-07-05: все 5 «флаков» основного checkout = «Test timed out in
 * 5000ms», ассертов-падений ноль (в чистом простаивающем worktree 157/157).
 */

// Без import 'vitest/config': проект запускается через npx-кэш (локального
// node_modules/vitest нет), а конфиг Node резолвит из node_modules/.vite-temp,
// откуда npx-инсталляция не видна → ERR_MODULE_NOT_FOUND. Плоский объект
// эквивалентен defineConfig и не тянет зависимостей.
export default {
  test: {
    testTimeout: 120_000,
  },
};
