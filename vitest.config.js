/** Геометрические испытания требуют большего времени, чем локальные unit-тесты. */
export default {
  test: {
    testTimeout: 120_000,
    // Вложенные рабочие деревья не принадлежат проверяемому checkout.
    exclude: ['**/node_modules/**', '**/dist/**', '**/.claude/**'],
  },
};
